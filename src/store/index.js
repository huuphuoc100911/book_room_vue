import { createStore } from "vuex";
import location from "./modules/location";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    location,
  },
});
